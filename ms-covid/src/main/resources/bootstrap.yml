spring:
  application:
    name: ms-covid
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      failFast: true

management:
  endpoints:
    web:
      exposure:
        include: '*'
