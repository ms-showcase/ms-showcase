package io.population;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativePopulationResourceIT extends PopulationResourceTest {

    // Execute the same tests but in native mode.
}